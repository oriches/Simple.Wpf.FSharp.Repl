<Window x:Class="Wpf.TestHarness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:themes="clr-namespace:Simple.Wpf.Themes;assembly=Simple.Wpf.Themes"
        xmlns:ui="clr-namespace:Simple.Wpf.FSharp.Repl.UI;assembly=Simple.Wpf.FSharp.Repl"
        Title="MainWindow"
        mc:Ignorable="d"
        Height="350"
        Width="525">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <themes:Themes x:Name="ThemeControl"
                       Margin="5"
                       Grid.Row="0"
                       Focusable="True"
                       FocusManager.FocusedElement="{Binding ElementName=ThemeControl}" />

        <ui:ReplWindow x:Name="ReplWindow"
                       StartUpScript="let ollie = 42;;"
                       Grid.Row="1" />
        
    </Grid>
</Window>
